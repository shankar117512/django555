# pyproject.toml (test configuration section)

[tool.coverage.run]
source = ["apps"]
omit = ["*/migrations/*", "*/tests/*", "*/conftest.py"]

[tool.coverage.report]
fail_under = 80
show_missing = true

[tool.isort]
profile = "black"
