# setup.cfg
# FIX: [coverage:report] fail_under was duplicated here AND in pyproject.toml.
# Two coverage config files with the same key causes unpredictable behaviour
# depending on which coverage.py loads last. Removed the duplicate here.
# pyproject.toml [tool.coverage.report] is now the single source of truth.

[coverage:run]
# intentionally empty — all coverage config lives in pyproject.toml
